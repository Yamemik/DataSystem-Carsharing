<UserControl x:Class="AutoDealer.TitleUC"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:AutoDealer" 
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Viewbox>
            <TextBlock Text="{Binding NameTest}" Width="100" Background="LightGreen" Foreground="Goldenrod" 
                       TextWrapping="Wrap" TextAlignment="Center"/>
        </Viewbox>
        <Button Grid.Row="1" Background="Green" Content="Начать тест" Command="{Binding JumpCommand}"/>
    </Grid>
</UserControl>
